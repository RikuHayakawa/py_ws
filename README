## 概要
vscode devcontainerでの使用を推奨
パッケージマネージャにはpoetry(https://python-poetry.org/docs/)を採用

## コンテナの立ち上げ
docker compose up -d

## ファイルの実行

docker exec -it <container-name> bash

cd src
poetry run python sample.py(指定のファイル)

## その他
gitコマンドはssh接続の必要なコマンド以外はコンテナ内で実行可能、ホストのsshはマウントしない設定


